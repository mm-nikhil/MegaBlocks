# Results

Curated results are grouped by run.

Current result:

- `nano-token-sweep/report.md`: RTX 3080 NanoMoE token sweep with
  `reference/cpu`, `reference/cuda`, and `megablocks_moe/cuda` rows.

Keep raw benchmark dumps under `results/raw/`; that path is ignored by git.
Summaries should follow `docs/reporting.md`.
